/**
 * @interface Sprites
 * Create the Sprites entity with its respective properties
 * @property { String } id - identifier @example 1
 */

export interface Sprites {
  front_default: string;
  front_shiny: string;
  front_female: string;
  front_shiny_female: string;
  back_default: string;
  back_shiny: string;
  back_female: string;
  back_shiny_female: string;
}
